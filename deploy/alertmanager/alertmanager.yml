route:
  receiver: default
  group_by: ["alertname"]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  # Plug in a real integration here (Slack, PagerDuty, email, webhook, ...).
  # Left empty on purpose for this demo project.
  - name: default
